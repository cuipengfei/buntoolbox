# Memory: Patterns

- For `mimic-webtop-kde-wsl`, research scope may expand to LinuxServer issues, Selkies source/issues, KDE/KWin/Wayland popup/focus/input, PulseAudio/null sink, XDG runtime, and Wayland socket lifecycle, but every external lead must be tied back to the root-cause question: why Docker Webtop KDE reference has stable right-click while WSL mimic right-click popup immediately disappears. Avoid random solution shopping.
