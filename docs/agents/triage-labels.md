# Triage labels

本仓库采用默认五个 canonical triage 标签，不做覆盖映射。

- `needs-triage` -> `needs-triage`
- `needs-info` -> `needs-info`
- `ready-for-agent` -> `ready-for-agent`
- `ready-for-human` -> `ready-for-human`
- `wontfix` -> `wontfix`

说明：

- `triage` 技能会按上面字符串读写标签。
- 若后续仓库标签体系变化，直接修改本文件即可，无需重跑 setup。

